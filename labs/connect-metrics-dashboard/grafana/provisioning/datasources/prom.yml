apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: cdc-prom            # dashboard's ${datasource} default resolves to this
    access: proxy
    url: http://prometheus:9090
    isDefault: true
