# 50k Loss-Free Headroom Bump — Design

**Lab:** `labs/redis-redpanda-throughput-stress/`
**Date:** 2026-05-27
**Status:** approved (brainstorm phase complete)

---

## 1. Goal

The 50k tier (both `batch` and `single` write modes) passes the harness verdict loss-free on the same 32-core / 122 GiB host where it currently fails, without changing the lab's identity, deliverables, or scope per the `/research-lab` skill.

"Passes loss-free" means, for both 50k runs:

- `verdict.detail.rate_floor_ok == true`
- `verdict.detail.missing_ok == true`
- `verdict.detail.p99_latency_ok == true` (after p99 ceiling is calibrated from the same matrix run)
- `missing == 0` and `trimmed == 0`

## 2. Root cause being addressed

The pipeline currently loses 40–50% of messages at 50k. The proximate cause is JetStream's `--max-bytes 2GB --discard old` policy: the `APP_EVENTS` stream fills mid-sustain and starts evicting unread messages before the sink can pull them.

The math:

- 50k msg/s × 30s sustain × ~1.7 KB JetStream envelope (payload + headers + framing) ≈ **2.55 GB peak buffer**.
- 2 GB cap < 2.55 GB peak → eviction starts mid-sustain.
- Observed end-of-run `nats.bytes` of 1.26 GB at 50k batch confirms the sink **does** keep pace once it isn't racing eviction; the residual is just the retained delivered messages still inside the file-backed stream.

Therefore: buffer is the only knob that needs to change. Sink CPU, `max_in_flight`, storage backend, and writer config are not bottlenecks at 50k and stay put.

## 3. Single change

Raise JetStream `--max-bytes` from `2GB` to `5GB` as the new lab default.

`5GB` is chosen as `~2× peak buffer at 50k`, giving comfortable headroom for clock-skew jitter and brief sink stalls without overcommitting disk. Smaller (4 GB) is tight; larger (8 GB) is unjustified at 50k and unrelated to the goal.

The knob becomes env-tunable for future research without docker-compose edits:

```yaml
--max-bytes ${NATS_MAX_BYTES:-5GB}
```

`.env.example` documents the knob. Default behavior with no override is 5 GB.

## 4. Files touched

| File | Change |
|---|---|
| `docker-compose.yml` | `--max-bytes 2GB` → `--max-bytes ${NATS_MAX_BYTES:-5GB}` in the `nats-init` stream-add command |
| `.env.example` | Add documented `NATS_MAX_BYTES=5GB` row |
| `README.md` | Add `NATS_MAX_BYTES` row to the knobs table; update the 50k narrative in the calibration-mode section (no longer "expected to fail") |
| `RESEARCH.md` | Add a "JetStream sizing" subsection documenting the 2 GB→5 GB rationale and the peak-buffer math |
| `scripts/lib/tier-defs.sh` | After verification run: set `TIER_P99_MS[50000]` from observed p99 (was `""`); revisit `TIER_RATE_MIN_PCT[50000]` only if the matrix shows headroom to tighten |
| `reports/*.json` | Regenerated by re-running the full matrix at the new cap |
| `reports/dashboard.html` | Regenerated by `scripts/dashboard.py` |

No changes to: writer/, collector/, connect/forward.yaml, connect/reverse.yaml, scripts/stress-run.sh, docker-compose service definitions other than the one `nats-init` command line.

## 5. Verification & calibration

The verification flow is also the calibration flow — one matrix run produces both the loss-free PASS and the data needed to set the 50k p99 ceiling.

1. `docker compose up --wait`; manually verify `docker exec rrts-nats nats stream info APP_EVENTS | grep -i max` shows the 5 GB cap before any tier runs.
2. Run the full default matrix: `bash scripts/stress-run.sh`. Expected wall-clock ~10–15 min.
3. **Expected first-pass result:** all 12 reports show `rate_floor_ok=true` and `missing_ok=true`. 50k both modes show `missing=0` and `trimmed=0`. p99 gate at 50k still reads `null` (skipped) because `TIER_P99_MS[50000]=""`.
4. Compute the 50k p99 ceiling using the established heuristic: `ceil_100ms(max(p99_batch, p99_single) × 1.25)`, floor 100ms. Write into `TIER_P99_MS[50000]`.
5. Re-run the full matrix once more to verify all 12 tiers green against the calibrated gate.
6. Regenerate the dashboard: `python3 scripts/dashboard.py`.

## 6. Verdict impact

- All 12 tiers expected to report `verdict.pass=true` after step 5.
- 5k/10k loose rate floors (`0.85`) and the 40k 8000 ms p99 ceiling stay as-is — those reflect real pipeline behavior unrelated to JetStream sizing.
- The 50k tier's rate floor stays at `0.90` unless the verification run shows comfortable headroom (e.g., `≥0.95` achieved across both modes) to justify a tightening — in which case bump per the existing tier-defs convention.

## 7. Risks

- **Higher p99 at 50k.** Bigger buffer means the sink may have a deeper queue to drain post-sustain, which could push p99 above the 40k baseline of ~6.4 s. If quiescence times out (`quiescence_timeout=true` in any 50k report), raise `DRAIN_S` from 10 to 20 in `scripts/lib/tier-defs.sh`, document the change in `RESEARCH.md`, and re-run. This is the only contingency the spec admits.
- **nats container memory pressure.** The container is capped at 2 GiB. A 5 GB file-backed stream indexes roughly 3 M messages; the in-memory index footprint is on the order of 500 MB. Comfortably under the cap, but `docker stats rrts-nats` should be eyeballed during the run. If RSS approaches the cap, bump nats `mem_limit` to 4 GiB.

## 8. Non-goals (explicit YAGNI)

- Not adding new tiers (60k, 80k). Out of scope for this change.
- Not switching JetStream from file to memory storage. Durability isn't load-bearing here, but changing it broadens blast radius without serving this goal.
- Not retuning connect-sink CPU/memory caps or `max_in_flight`. The bytes accounting shows the sink is not the bottleneck at 50k.
- Not modifying the writer, collector, Bloblang mappings, or stream `MAXLEN` settings.
- Not adding profiles (`--profile=high-throughput`) or matrix variants. The 5 GB cap is the single new default.

## 9. /research-lab compliance checklist

The lab still satisfies the `/research-lab` skill output contract:

- `RESEARCH.md` — present, updated with JetStream-sizing subsection
- `README.md` — present, updated knobs table and 50k narrative
- Design spec — this document
- Implementation plan — to be created next via `superpowers:writing-plans`
- Working `docker-compose.yml` stack — yes, same stack with one config knob changed
- Writer / collector services — unchanged
- `scripts/stress-run.sh` — unchanged
- `reports/*.json` — regenerated with new cap
- Verdict gating — unchanged structure; 50k now passes all three gates after calibration

No additions or removals to lab deliverables. The change is a single configuration value plus the calibration and documentation work that follows from it.
