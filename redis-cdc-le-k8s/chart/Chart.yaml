apiVersion: v2
name: redis-cdc-le-k8s
description: Redis -> Redpanda Connect -> NATS JetStream CDC chart with K8s-Lease leader election (active/standby) on both connect legs and a pre-created pull consumer
type: application
version: 0.1.0
appVersion: "1.0.0"
